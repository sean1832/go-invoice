# ---------------------------------
# 1. SERVER SETTINGS
# ---------------------------------
# These are the main settings for running the app.

# Port to run the server on.
PORT=8080

# The full public URL where this server will be accessible.
# (e.g., http://localhost:8080 or https://invoices.your-domain.com)
PUBLIC_URL="http://localhost:8080"

# Where to store data files (invoices, clients, etc.).
# Default is a 'db' folder next to the executable.
# !! For production, use an absolute path:
#    (e.g., /var/lib/go-invoice or C:\ProgramData\go-invoice)
# you can still override this with `--db` flag when starting the server.
STORAGE_PATH=./db


# ---------------------------------
# 2. PRODUCTION & SESSION
# ---------------------------------
# CRITICAL settings for a live, public server.

# Set to "true" when in production.
IS_PROD="false"

# !! SET THIS FOR PRODUCTION !!
# A random, persistent secret for sessions.
# Prevents users from being logged out on every server restart.
# (Generate with: openssl rand -base64 32)
SESSION_SECRET=""

# Session duration in seconds.
# (Default 30 days: 86400 * 30)
SESSION_MAX_AGE=2592000


# ---------------------------------
# 3. EMAIL (OPTIONAL)
# ---------------------------------
# Configure this *only* if you want to send invoices from the app.
# Choose ONE method. Method 1 (OAuth) is recommended.

# --- METHOD 1: Google OAuth2 (Recommended) ---
# See "docs/email-setup-oauth2.md"
GOOGLE_OAUTH_CLIENT_ID=""
GOOGLE_OAUTH_CLIENT_SECRET=""
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587


# --- METHOD 2: App Password (Simple, less secure) ---
# See "docs/email-setup-app-password.md"
# To use this, leave the GOOGLE_OAUTH fields above BLANK.
#
# SMTP_FROM="" # e.g. "your-email@gmail.com"
# SMTP_PASSWORD="" # App Password generated in Google Account "https://myaccount.google.com/apppasswords"
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587


# ---------------------------------
# 4. DEVELOPMENT ONLY
# ---------------------------------
# (Ignore this for normal use)
#
# Uncomment if you are running the Svelte dev server separately
# to allow requests from the Go backend.
# DEV_FRONTEND_BASE_URL="http://localhost:5173"